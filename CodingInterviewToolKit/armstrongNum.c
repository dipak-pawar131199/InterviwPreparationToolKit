//Armustrong number def=> if the sum of the cubes of its digits is equal to the number itself
# include<stdio.h>
#include<stdlib.h>
void main(int nags,char *argv[])
{
    int n=atoi(argv[1]);
    int lastdigit,
    printf("%d",n);

    return;
}